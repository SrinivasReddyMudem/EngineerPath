GROQ_API_KEY=your-key-here

# Optional fallback keys — tried in order if the previous one hits its
# daily rate limit (each Groq account has its own separate 500k TPD quota).
# GROQ_API_KEY_2=your-second-account-key-here
# GROQ_API_KEY_3=your-third-account-key-here
